I want you to fix and improve the product creation system without destroying the existing structure.

PDF Generation

- The current PDF output is poor quality. I want the PDF generation to use AI, not just code-based templates.
- Use Gemini (or any AI model selected in the Admin settings) to generate the content and structure.
- Design the PDF in a premium style with professional formatting.
- Include graphics and relevant images based on the niche and the product topic.
- Images should not be generated by code placeholders. They should come either from:
  - Gemini image generation, or
  - A stock image provider selected in the Admin settings.
- Every PDF must include a premium cover page inside the PDF.
- Generate a powerful and attractive cover page with a product image relevant to the niche.

Marketing Guide

The marketing guide is missing. Please fix and expand it.

Generate a complete marketing package including:

- Facebook marketing guide
- Facebook Ads guide
- YouTube marketing guide
- TikTok marketing guide
- Premium promotion strategies
- Sales scripts
- Ad copy examples
- Content ideas
- CTA suggestions

Email Sequence

Make sure email sequences are generated correctly.

Requirements:

- Generate a complete 30-day email sequence.
- Users should be able to open and view the email sequence.
- Users should be able to download the email sequence.
- Users should be able to download the generated PDF.
- Users should also be able to publish the product immediately after generation.

AI Integration

Do not change the existing structure of the system.

Improve and stabilize the Gemini integration because sometimes product generation fails with "API key is not set", even though the API key has already been configured.

Requirements:

- Verify that the API key is correctly loaded from the Admin settings.
- Ensure all generation features use the selected AI provider.
- Add proper fallback and error handling.
- Prevent false "API key not set" errors.
- Make the connection stable and reliable.

Upgrade the system while preserving the current architecture and functionality.